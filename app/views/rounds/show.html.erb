<h1>Rounds#show</h1>

<p>Ici c'est la page de jeu</p>
<p>end_page to be displayed and the "game_banner" with the counter </p>
<p>modal pop up when counter arrives to 5 or 3 </p>
<p>Puis quand le jeu est fini, on a le choix de:</p>

<!-- Tracking the mouse with coordinates---------- -->
  <!-- <div id="top-mouse">Top mouse : 0</div>
  <div id="left-mouse">Left mouse : 0</div> -->
<!-- --------------------------------------------- -->

<%# if current_user == @game_session.user_id %>
  <%#= link_to "Play a new Round", new_game_session_round_path(@game_session) %>
<%# else %>
  <%#= link_to "Play a new Round", game_session_path(@game_session) %>
<%# end %>
<%#= link_to "End the Game Session", game_session_rounds_path(@game_session) %>

<div class="container">
  <div id="game-container">
    <div id="wikipage-container" data-start-page="<%= @round.start_page %>" data-end-page="<%= @round.end_page %>">

      <!-- <div id="portal-test" class="small-portal"></div> -->
    </div>
  </div>
</div>
